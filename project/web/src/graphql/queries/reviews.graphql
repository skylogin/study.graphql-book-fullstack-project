query cutReviewsByUser($cutReviewsByUserId: Int!) {
  cutReviewsByUser(id: $cutReviewsByUserId) {
    id
    contents
    cutId
    user {
      username
      email
    }
    createdAt
    updatedAt
  }
}
